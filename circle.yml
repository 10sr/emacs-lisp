dependencies:
  pre:
    - sudo add-apt-repository -y ppa:cassou/emacs
    - sudo apt-get update -qq
    - sudo apt-get install -qq emacs24 emacs-snapshot

test:
  override:
    - make test emacs=emacs-snapshot
    - make test emacs=emacs24
